/**
 * The attendee PWA. It has no router — every screen lives behind the same
 * origin — so public links point at the root and the app decides where to land.
 */
export const ATTENDEE_APP_URL = 'https://app.replaycon.in';
